import MainLayout from "../../layouts/MainLayout";
import styles from "./index.module.scss";

const HomePage = () => {
  return <MainLayout>временно тут ничего не будет</MainLayout>;
};

export default HomePage;
